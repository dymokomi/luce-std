#prisma 4.0
def package "file_io_contracts" {
    def native "inputs" {
        str[] sources = ["faults.c"]
    }

    def dependency "luce-std" {
        str owner = "dymokomi"
        str version = "^0.1.0"
        str path = "../../.."
    }
}
